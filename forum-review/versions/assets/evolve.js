const menu=document.querySelector('.menu-button');
const nav=document.getElementById('navigation');
function closeMenu(){nav.classList.remove('open');menu.setAttribute('aria-expanded','false');menu.setAttribute('aria-label','展开导航')}
menu.addEventListener('click',()=>{const open=!nav.classList.contains('open');nav.classList.toggle('open',open);menu.setAttribute('aria-expanded',String(open));menu.setAttribute('aria-label',open?'收起导航':'展开导航')});
nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));
document.addEventListener('keydown',e=>{if(e.key==='Escape'){closeMenu()}});
const filters=[...document.querySelectorAll('[data-filter]')];
function filterProgram(button){filters.forEach(b=>{const active=b===button;b.setAttribute('aria-selected',String(active));b.tabIndex=active?0:-1});document.querySelectorAll('[data-phase]').forEach(item=>{item.hidden=button.dataset.filter!=='all'&&item.dataset.phase!==button.dataset.filter})}
filters.forEach((button,i)=>{button.addEventListener('click',()=>filterProgram(button));button.addEventListener('keydown',e=>{let n;if(e.key==='ArrowRight')n=(i+1)%filters.length;else if(e.key==='ArrowLeft')n=(i+filters.length-1)%filters.length;else if(e.key==='Home')n=0;else if(e.key==='End')n=filters.length-1;else return;e.preventDefault();filterProgram(filters[n]);filters[n].focus()})});
let draft='';
document.getElementById('inquiry-form').addEventListener('submit',e=>{e.preventDefault();const data=new FormData(e.currentTarget);draft=['生态文明与发展论坛参会咨询','',`姓名：${data.get('name')}`,`联系电话：${data.get('phone')}`,`单位 / 机构：${data.get('organization')||'未填写'}`,`关注的活动：${data.get('session')}`,`咨询事项：${data.get('message')||'希望了解参会安排。'}`].join('\n');document.getElementById('mail-body').textContent=draft;document.getElementById('open-mail').href='mailto:xzq_789@163.com?subject='+encodeURIComponent('2026生态文明与发展论坛参会咨询')+'&body='+encodeURIComponent(draft);const preview=document.getElementById('mail-preview');preview.hidden=false;document.getElementById('draft-status').textContent='收件人：xzq_789@163.com。请检查内容后，自行发送邮件。';preview.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth',block:'nearest'})});
document.getElementById('copy-mail').addEventListener('click',async()=>{try{await navigator.clipboard.writeText(draft);document.getElementById('draft-status').textContent='内容已复制，可粘贴到邮件中发送至 xzq_789@163.com。'}catch{const range=document.createRange();range.selectNodeContents(document.getElementById('mail-body'));const selection=window.getSelection();selection.removeAllRanges();selection.addRange(range);document.getElementById('draft-status').textContent='请复制已选中的草稿内容，发送至 xzq_789@163.com。'}});
const reduced=matchMedia('(prefers-reduced-motion: reduce)');
if('IntersectionObserver'in window&&!reduced.matches){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('seen');observer.unobserve(entry.target)}}),{threshold:.06});document.querySelectorAll('.section-heading,.vision-layout,.pillars-grid,.pathway-layout,.growth-grid,.eco-callout,.join-layout').forEach(el=>{el.classList.add('reveal');observer.observe(el)});reduced.addEventListener('change',()=>{if(reduced.matches){document.querySelectorAll('.reveal').forEach(el=>el.classList.add('seen'));observer.disconnect()}})}
